# Slate VS Code extension playground

Open `App.slate` to test the local Slate extension.

Expected local DX:

- Slate template syntax highlighting.
- TypeScript highlighting inside `<script slate>`.
- Diagnostics from the Slate language server.
- Hover, completion, definition, and document symbols.
